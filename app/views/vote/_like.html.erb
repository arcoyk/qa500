<div class='vote-button-set'>
	<%= link_to '', like_path({:id => vote.id}), :class => 'vote-button yes-button', :method => :post, :remote => true %>
	<br>
	<b class='num' id='like-<%= vote.id %>' >
		<%= vote.like %>
	</b>
	PUSH
</div>